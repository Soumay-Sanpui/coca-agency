import React from 'react'

const FilledBtn = ({text}) => {
  return (
    <button className='rounded-full px-[3vw] p-3 bg-btncl text-white font-semibold'>{text}</button>
  )
}

export default FilledBtn